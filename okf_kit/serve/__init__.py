"""okf serve — a local HTTP API that wraps registry / read / chat / settings so
GUIs (the desktop app, calknowledge's web UI) are pure UI over an API. Needs the
`[serve]` extra:  pip install "okf-kit[serve]".
"""
